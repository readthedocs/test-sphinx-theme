import 'happo-cypress';
import { configure } from 'happo-cypress';

/* Disabling for now, this seems to cause issues with Happo targets. It was
 * originally enabled to get stylesheets at Happo from our local sources. */
/*
configure({
  localSnapshots: true
});
*/
